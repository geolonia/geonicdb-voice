# GeonicDB サーバーの URL
VITE_GEONICDB_URL=https://geonicdb.geolonia.com

# 書き込み専用 API キー (gdb_ で始まる文字列)
# `geonic me api-keys create` の出力。作成時にしか表示されない
VITE_GEONICDB_API_KEY=

# テナント名。UUID の tenantId ではない
# `geonic me` の出力に含まれる tenantName で確認できる
VITE_GEONICDB_TENANT=
